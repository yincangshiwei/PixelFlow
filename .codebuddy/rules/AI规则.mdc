---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-08-07T12:00:00.000Z
provider: 
---

# 项目 AI 操作约束规则

## 依赖安装与项目运行规则

### 基本原则

- AI **禁止主动**执行 `pip install`、`uv pip install`、`npm install`、`conda install` 等任何依赖安装命令；安装必须由用户本人执行
- 需要安装时，AI 只以文字形式给出安装命令与目标环境说明，不得代为执行
- **严禁**自主启动、运行项目（如 `python app.py`、`npm run dev` 等），项目运行由用户自行负责

### 项目依赖（项目已声明，如 requirements.txt 中的包）

- 发现缺失时，提示用户安装到**项目虚拟环境**（本项目为 `I:\dev_works\python_venv\PixelFlow`），不得代为执行

### 非项目依赖（项目未声明的辅助工具，如 lint / 检查脚本等）

- **禁止**安装进项目虚拟环境，避免污染项目环境、影响打包产物
- 优先不使用（用标准库或项目已有能力替代）；确需使用时，提示用户安装到**用户系统环境**（如 `pip install --user <pkg>`、`uv tool install <pkg>`），再调用系统环境中的可执行入口

## 数据库变更规则

- 所有涉及数据库结构或数据的改动（建表、加字段、改字段、删表、初始化数据等），**不得直接操作数据库**，必须以 SQL 语句的形式提供给用户
- 提供 SQL 前，先检查项目根目录及子目录中是否存在 `.sql` 文件：
  - **存在 `.sql` 文件**：将新增的 SQL 语句追加到该文件的末尾（按变更日期顺序），并附上注释说明变更内容和日期，格式如下：
    ```sql
    -- [YYYY-MM-DD] 变更说明
    SQL语句;
    ```
  - **不存在 `.sql` 文件**：将 SQL 语句以代码块形式直接提供给用户，并说明建议保存的文件路径
